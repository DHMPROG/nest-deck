/**
 * Pure SPA: the Nest Hub stores nothing and all data comes from the backend
 * at runtime, so there is nothing meaningful to prerender or server-render.
 */
export const ssr = false;
export const prerender = false;
