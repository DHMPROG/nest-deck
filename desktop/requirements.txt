# Everything needed to run the Nest Deck desktop app from source.
#   python -m venv .venv
#   .\.venv\Scripts\pip install -r desktop/requirements.txt
# Then build the frontend once (npm run build in frontend/) and:
#   .\.venv\Scripts\python desktop/app.py

# --- backend ---
fastapi>=0.115
uvicorn[standard]>=0.30
sqlmodel>=0.0.22
sse-starlette>=2.1

# --- casting ---
pychromecast>=14

# --- service integrations (optional, but imported lazily so harmless) ---
obsws-python>=1.7
spotipy>=2.24
pynput>=1.7

# --- desktop window ---
pywebview>=5
